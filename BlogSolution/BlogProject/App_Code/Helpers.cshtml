@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<BlogProject.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@BlogProject.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}